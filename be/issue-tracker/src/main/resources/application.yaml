spring:
  profiles:
    active: local #default
  application:
    name: issue-tracker
---
spring:
  config:
    activate:
      on-profile: local

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/issue-tracker
    username: sa
---
spring:
  config:
    activate:
      on-profile: prod
#  datasource:
---
